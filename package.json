{
  "name": "codex-wechat-channel",
  "version": "0.1.2",
  "description": "Bridge WeChat ClawBot messages into Codex app-server threads",
  "type": "module",
  "bin": {
    "codex-wechat-channel": "./dist/cli.js"
  },
  "files": [
    "dist/cli.js",
    "dist/src/",
    "dist/scripts/",
    "docs/plan-and-progress.md",
    "docs/releases/",
    "README.md"
  ],
  "scripts": {
    "start": "npm run build && node dist/cli.js start",
    "setup": "npm run build && node dist/cli.js setup",
    "clean": "node tools/clean-build.mjs",
    "build": "npm run clean && tsc -p tsconfig.json && node tools/copy-runtime-assets.mjs",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "prepack": "npm run build",
    "probe": "npm run build && node dist/cli.js probe",
    "bridge:start": "npm run build && node dist/cli.js bridge start",
    "bridge:status": "npm run build && node dist/cli.js bridge status",
    "bridge:stop": "npm run build && node dist/cli.js bridge stop",
    "bridge:probe": "npm run build && node dist/cli.js probe",
    "service:install": "npm run build && node dist/cli.js service install",
    "service:status": "npm run build && node dist/cli.js service status",
    "service:uninstall": "npm run build && node dist/cli.js service uninstall",
    "audit:code": "npm run build && node dist/tools/maintenance-report.js"
  },
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "codex",
    "wechat",
    "channel",
    "app-server",
    "openai"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/renqingfei/codex-wechat-channel.git"
  },
  "bugs": {
    "url": "https://github.com/renqingfei/codex-wechat-channel/issues"
  },
  "homepage": "https://github.com/renqingfei/codex-wechat-channel#readme",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^6.0.3"
  }
}
